package com.example.goodmarksman.enams;

public enum Action {
    NULL,
    GAME_STARTED,
    GAME_STOPPED,
    CLIENT_CONNECTED,
    CLIENT_DISCONNECTED,
    CONNECT,
    CONNECTION_ERROR,
    CLIENT_STATE,
    UPDATE_GAME_STATE,
    SET_NAME,
    WIDTH_INIT,
    SHOT,
    GET_DB
}
